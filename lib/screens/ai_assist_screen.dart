import 'package:flutter/material.dart';

class AiAssistScreen extends StatelessWidget {
  const AiAssistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AI Assist coming soon'),
      ),
    );
  }
}
